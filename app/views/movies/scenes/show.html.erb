<%= content_for :breadcrumbs do
  breadcrumbs([[movie_path(@movie), @movie.title], [movie_scenes_path(@movie), "scenes"], [movie_role_path(@movie, @scene), @scene.name]])
end
%>
<%= content_for :title do %>
  <%= @scene.name %>
<% end %>
