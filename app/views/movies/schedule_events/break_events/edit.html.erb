<%= content_for :title do %>
  New Event
<% end %>

<%= render partial: "movies/schedule/events/#{@event.class.name.underscore}", schedule: @schedule, movie: @movie, schedule_event: @schedule_event %>
