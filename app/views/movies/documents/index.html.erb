<%= content_for :breadcrumbs do
  breadcrumbs([[movie_path(@movie), @movie.title], [movie_documents_path(@movie), "Locations"]])
end
%>
<%= content_for :title do %>
  Documents
<% end %>

Documents are not supported at the time being.
